#ifndef BISPEC_H
#define BISPEC_H

#include <stdio.h>
#include "adata.h"

extern int make_bispec (trianglesum *, fringesum *, int, char *);
extern int parse_cmdline (int, char **, FILE **, FILE **, char *);

#endif
